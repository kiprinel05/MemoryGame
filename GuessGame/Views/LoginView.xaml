<Window x:Class="GuessGame.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="500" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select a user:" FontWeight="Bold" FontSize="16" Margin="0 0 0 10"/>

        <ListBox x:Name="UserListBox" Grid.Row="1" DisplayMemberPath="Name" SelectionChanged="UserListBox_SelectionChanged"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10">
            <Button Content="New User" Click="NewUser_Click" Margin="5"/>
            <Button Content="Edit User" Click="EditUser_Click" Margin="5"/>
            <Button Content="Delete User" Click="DeleteUser_Click" Margin="5"/>
            <Button Content="Login" Click="Login_Click" Margin="5"/>
        </StackPanel>

        <!-- User Form -->
        <StackPanel x:Name="UserFormPanel" Grid.Row="3" Visibility="Collapsed" Background="#FFEFEFEF" Margin="0 10 0 0">
            <TextBlock Text="Name:"/>
            <TextBox x:Name="NameTextBox" Margin="0 5 0 10"/>

            <TextBlock Text="Avatar:"/>
            <Image x:Name="AvatarImage" Height="100" Margin="0 5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5">
                <Button Content="b" Width="40" Click="PrevAvatar_Click"/>
                <Button Content="a" Width="40" Click="NextAvatar_Click" Margin="10 0 0 0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5">
                <Button Content="Save" Click="SaveUser_Click" Margin="5"/>
                <Button Content="Cancel" Click="CancelUser_Click" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
