<Window x:Class="GuessGame.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GuessGame.ViewModels"
        mc:Ignorable="d"
        Title="Login" Height="500" Width="400">
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select a user:" FontWeight="Bold" FontSize="16" Margin="0 0 0 10"/>

       <ListBox Grid.Row="1" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}">
    <ListBox.ItemTemplate>
        <DataTemplate>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Image Source="{Binding AvatarPath}" Width="40" Height="40" Margin="0 0 10 0"/>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="14"/>
            </StackPanel>
        </DataTemplate>
    </ListBox.ItemTemplate>
</ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10">
            <Button Content="New User" Command="{Binding AddUserCommand}" Margin="5"/>
            <Button Content="Edit User" Command="{Binding EditUserCommand}" Margin="5"/>
            <Button Content="Delete User" Command="{Binding DeleteUserCommand}" Margin="5"/>
            <Button Content="Login" Command="{Binding PlayCommand}" Margin="5"/>
        </StackPanel>

        <!-- User Form -->
        <StackPanel Grid.Row="3" Visibility="{Binding UserFormVisibility}" Background="#FFEFEFEF" Margin="0 10 0 0">
            <TextBlock Text="Name:"/>
            <TextBox Text="{Binding EditingUser.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0 5 0 10"/>

            <TextBlock Text="Avatar:"/>
            <Image Source="{Binding CurrentAvatar}" Height="100" Margin="0 5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5">
                <Button Content="⟨" Width="40" Command="{Binding PrevAvatarCommand}"/>
                <Button Content="⟩" Width="40" Command="{Binding NextAvatarCommand}" Margin="10 0 0 0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5">
                <Button Content="Save" Command="{Binding SaveUserCommand}" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelEditCommand}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
